Some features which are missing or require work:

(Ordered by Priority)

# Add a Dynamic Table for Games up to 6x6
# Add persistent storage for the Session Token, i.e. inbetween disconnects or restarts
# Currently, it is impossible to distinguish Games with the same name
# Some controls are missing proper CSS.